test:
  override:
    - python3.6 -m unittest tests.test_my_air_cargo_problems:

  post:
    - python3.6 ./run_search.py -p 1 2 3 -s 6 7 8 9:
        timeout: 9000

machine:
  python:
    version: 3.6.0
